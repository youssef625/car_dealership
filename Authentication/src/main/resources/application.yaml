server:
  port: 8080

spring:
  application:
    name: Authentication
  datasource:
    url: jdbc:mysql://localhost:3306/car_dealership_db
    username: root
    password: yourpassword
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
  flyway:
    enabled: true
    baseline-on-migrate: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

jwt:
  secret: aaf4c61ddcc5e8a2dabede0f3b482cd9aea9434d
  expiration: 86400000

